# 💻 내 컴퓨터에서 실시간으로 보면서 편집하기

파일을 저장하면 브라우저가 **자동으로 새로고침**됩니다 (핫 리로드).
아래 순서대로 한 번만 세팅하면 됩니다.

## 0. Node.js 설치 (최초 1회)
- https://nodejs.org 접속 → **LTS 버전** 다운로드 → 설치 (계속 "다음")
- 설치 확인: 명령 프롬프트(cmd)에서 `node -v` 입력 → 버전이 나오면 성공

## 1. 코드 내려받기
**방법 A — GitHub에서 ZIP 다운로드 (제일 쉬움)**
1. GitHub 저장소에서 브랜치를 **`claude/magical-cannon-09v2yz`** 로 변경
2. 초록색 **`Code ▾`** 버튼 → **Download ZIP**
3. 다운로드한 ZIP을 압축 풀기 (예: 바탕화면)

**방법 B — Git 사용 (Git 설치돼 있으면)**
```bash
git clone -b claude/magical-cannon-09v2yz https://github.com/sten5891-code/claude.git
```

## 2. 실행
**Windows: `start.bat` 더블클릭** → 자동으로 설치 + 서버 실행
(처음 한 번은 패키지 설치로 1~2분 걸립니다)

또는 직접 명령어로:
```bash
npm install        # 최초 1회
copy .env.example .env.local   # 최초 1회 (Mac/Linux: cp .env.example .env.local)
npm run dev
```

## 3. 브라우저에서 열기
주소창에 **http://localhost:3000** 입력 → 사이트 등장 🎉

## 4. 보면서 편집하기 (핵심!)
- 추천 편집기: **VS Code** (https://code.visualstudio.com)
- VS Code로 이 폴더를 열고 → 화면을 반반 띄워두기
  (왼쪽 VS Code 코드, 오른쪽 브라우저)
- 파일을 고치고 **저장(Ctrl+S)** 하면 → 브라우저가 즉시 바뀝니다.

### 자주 바꾸는 곳
| 바꾸고 싶은 것 | 파일 |
| --- | --- |
| 상품명·가격·설명·사이즈표 | `src/lib/data/products.ts` |
| 상품 사진 | `public/images/product-1.png` |
| 색상·디자인 톤 | `tailwind.config.ts`, `src/app/globals.css` |
| 홈 첫 화면 문구 | `src/components/home/Hero.tsx` |
| 푸터(회사정보) | `src/components/layout/Footer.tsx` |

## 종료
서버 실행 중인 창에서 **Ctrl + C**

---
> ⚠️ 편집한 내용을 GitHub에도 저장하려면 `git add . && git commit -m "수정" && git push` 가 필요합니다.
> Git이 어렵다면, 바꾼 파일을 GitHub 웹에서 직접 수정해도 됩니다.
